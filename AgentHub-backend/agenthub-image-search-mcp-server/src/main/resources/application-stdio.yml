spring:
  ai:
    mcp:
      server:
        name: agenthub-image-search-mcp-server
        version: 0.0.1
        type: SYNC
        # stdio
        stdio: true
  # stdio
  main:
    web-application-type: none
    banner-mode: off
logging:
  level:
    root: OFF
